import { palette } from "shared/constants/palette";

/**
 * Shared scrollbar style values used across scrolling frames.
 * Pass these as props to any <scrollingframe> for consistent styling.
 */
export const SCROLLBAR_COLOR = palette.blue;
export const SCROLLBAR_TRANSPARENCY = 0.9;
export const SCROLLBAR_THICKNESS = 1;
